# Assembly Drawing Tool (minimal scaffold)

This is a minimal scaffold for the Assembly Drawing Tool described by the user.

Quick start (Windows PowerShell):

```powershell
python -m venv .venv; .\.venv\Scripts\Activate.ps1; pip install -r requirements.txt; python app.py
```

Open http://127.0.0.1:5000 in your browser.
